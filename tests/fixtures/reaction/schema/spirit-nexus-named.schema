{}
{
  Vector Vector
  Optional Optional
  ScopeOf ScopeOf
  Map Map
  Bytes FixedBytes
  Work (Frame [Event WriteDone ReadDone EffectDone] [(SignalArrived Event) (SemaWriteCompleted WriteDone) (SemaReadCompleted ReadDone) (EffectCompleted EffectDone)])
  Action (Frame [Reply Write Read Effect Continuation] [(ReplyToSignal Reply) (CommandSemaWrite Write) (CommandSemaRead Read) (CommandEffect Effect) (Continue Continuation)])
}
[]
[]
{

  SignalInput { String }
  SignalOutput { String }
  SemaReadInput { String }
  SemaReadOutput { Integer }
  SemaWriteOutput { Boolean }

  SemaWriteInput [(Record SignalInput)]

  NexusEffectCommand [(Stash SignalInput)]
  NexusEffectResult [(Stashed SignalOutput)]

  NexusWork Work.(SignalInput SemaWriteOutput SemaReadOutput NexusEffectResult)
  NexusAction Action.(SignalOutput SemaWriteInput SemaReadInput NexusEffectCommand NexusWork)
}
