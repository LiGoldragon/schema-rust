{}
{
  Vector Vector
  Optional Optional
  ScopeOf ScopeOf
  Map Map
  Bytes FixedBytes
}
[(Record Entry) (Subscribe SubscribeIntent opens IntentEventStream)]
[(RecordAccepted RecordIdentifier) (SubscriptionStarted IntentSubscription) (Event IntentEvent)]
{
  Topic String
  Topics Vector.Topic
  Description String
  RecordIdentifier Integer
  SubscriptionToken Integer

  SubscribeIntent Query

  Entry { Topics Description }
  Query { Topics }
  IntentSubscription { SubscriptionToken }
  IntentRecorded { Entry RecordIdentifier }
  IntentEvent [(Recorded IntentRecorded belongs IntentEventStream)]
  IntentEventStream (Stream { token.SubscriptionToken opened.SubscriptionStarted event.IntentEvent close.SubscriptionToken })
}
